# Plan for Issue #4

- [ ] Implement Redis/memory caching for chat members in `broadcast`
- [ ] Invalidate/update cache when a new chat is created (members join)
- [ ] Invalidate/update cache when a member leaves a chat
- [ ] Invalidate/update cache when a member is added to a chat (if applicable)
