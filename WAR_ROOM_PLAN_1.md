# Implementation Plan: Issue 1

- [ ] Create `uploads` directory and mount it as StaticFiles in FastAPI for serving.
- [ ] Add `UploadFile` endpoint `/upload` handling `image` and `file` types, including file size and type validation.
- [ ] Update frontend or test endpoints (if applicable - currently backend focus).
